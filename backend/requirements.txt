# Mi Portafolio — dependencias backend (producción)
# Pin de versiones para builds reproducibles

# Core web
Flask>=3.0,<4.0
flask-cors>=6.0         # >=6.0: corrige CVE-2024-6221/6839/6844/6866 (matching CORS)
flask-compress>=1.14    # gzip/brotli automático en JSON/HTML (~80% reducción)
gunicorn>=23.0  # >=23.0: corrige CVE-2024-1135 y CVE-2024-6827 (HTTP request smuggling)

# Análisis cuantitativo
numpy>=1.26,<3.0
pandas>=2.1
scipy>=1.11
yfinance>=0.2.59       # versiones modernas resuelven el "crumb" de Yahoo vía curl_cffi
curl_cffi>=0.7         # impersona un navegador real → evita el 401 "Invalid Crumb" de Yahoo

# HTTP cliente
requests>=2.32.4       # >=2.32.4: corrige CVE-2024-35195 y CVE-2024-47081 (fuga de credenciales)

# Reportes PDF
reportlab>=4.0

# Postgres (production DB)
psycopg[binary]>=3.1,<4.0

# Observabilidad
sentry-sdk[flask]>=2.8         # >=2.8: corrige CVE-2024-40647 (fuga de env vars a subprocesos)

# Rate limiting
Flask-Limiter>=3.5

# Variables de entorno
python-dotenv>=1.0

# Web Push notifications (iOS 16.4+, Android, Desktop)
pywebpush>=2.0
cryptography>=44.0             # >=44.0: incluye fix de CVE-2024-26130 y parches recientes
